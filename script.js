const vat = 5 / 100;
let baseP = 100;
const incr = 50;

console.log(
  "Base price:",
  baseP,
  "Total price after VAT:",
  baseP + baseP * vat,
);
baseP = baseP + incr;
console.log(
  "Base price:",
  baseP,
  "Total price after VAT:",
  baseP + baseP * vat,
);
baseP = baseP + incr;
console.log(
  "Base price:",
  baseP,
  "Total price after VAT:",
  baseP + baseP * vat,
);
baseP = baseP + incr;
console.log(
  "Base price:",
  baseP,
  "Total price after VAT:",
  baseP + baseP * vat,
);
baseP = baseP + incr;
