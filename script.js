let vat = 5 / 100;
let baseP = 100;

console.log(
  "Base price:",
  baseP,
  "Total price after VAT:",
  baseP + baseP * vat,
);
baseP = baseP + 50;
console.log(
  "Base price:",
  baseP,
  "Total price after VAT:",
  baseP + baseP * vat,
);
baseP = baseP + 50;
console.log(
  "Base price:",
  baseP,
  "Total price after VAT:",
  baseP + baseP * vat,
);
baseP = baseP + 50;
console.log(
  "Base price:",
  baseP,
  "Total price after VAT:",
  baseP + baseP * vat,
);
baseP = baseP + 50;
